#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>

using namespace std;

#define ll long long

void solve() {
    int n, k;
    cin >> n >> k;

    int ans = ( (n-1 ) * k) + 1;
  cout << ans << '\n';

    
}

int main() {
    ll t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}
